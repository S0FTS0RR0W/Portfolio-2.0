---
const socialLinks = [
  {
    platform: "GitHub",
    url: "https://github.com/S0FTS0RR0W",
    label: "Check out my code"
  },
  {
    platform: "LinkedIn",
    url: "https://linkedin.com",
    label: "Connect professionally"
  },
  {
    platform: "Twitter",
    url: "https://twitter.com",
    label: "Follow my updates"
  }
];
---

<div class="container">
  <div class="contact-wrapper">
    <h1>Get in Touch</h1>
    <p class="intro">
      Feel free to reach out or follow me on social media.
    </p>

    <div class="social-grid">
      {socialLinks.map((link) => (
        <a href={link.url} target="_blank" rel="noreferrer" class="social-card">
          <div class="card-content">
            <h3>{link.platform}</h3>
            <p>{link.label}</p>
          </div>
          <div class="arrow">&nearr;</div>
        </a>
      ))}
    </div>
  </div>
</div>

<style>
  .contact-wrapper {
    padding: 4rem 0;
    max-width: 600px;
  }
  h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }
  .intro {
    color: var(--muted);
    margin-bottom: 2rem;
    font-size: 1.1rem;
  }
  .social-grid {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  .social-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem;
    border: 1px solid var(--border-color);
    border-radius: 8px;
    text-decoration: none;
    color: inherit;
    transition: all 0.2s;
  }
  .social-card:hover {
    border-color: var(--text-color);
    background: rgba(255,255,255,0.03);
  }
  .card-content h3 {
    margin: 0;
    font-size: 1.2rem;
  }
  .card-content p {
    margin: 0.25rem 0 0;
    color: var(--muted);
    font-size: 0.9rem;
  }
  .arrow {
    font-size: 1.25rem;
  }
</style>
